# Core FastAPI backend
fastapi==0.104.1
uvicorn[standard]==0.24.0
aiohttp==3.9.1
aiofiles==23.2.1
python-multipart==0.0.6
pydantic==2.5.0
websockets==12.0
requests==2.31.0

# Document processing (Python 3.13 compatible)
python-pptx==0.6.23
PyPDF2==3.0.1
Pillow>=10.2.0

# PDF processing alternative (compatible)
pypdf==3.0.1

# AI/ML for analysis (OpenAI as fallback)
openai==1.3.0

# Basic image processing (compatible)
numpy>=1.24.0

# Optional: Add these later if needed
# pdf2image==1.17.0  # Requires poppler-utils system dependency
# pytesseract==0.3.10  # Requires tesseract system dependency
# PyMuPDF==1.23.0  # Alternative PDF processor
# opencv-python-headless==4.8.0  # For video processing
# moviepy==1.0.3  # For video manipulation